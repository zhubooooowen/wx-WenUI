<view class='album'>
  <view class='item' bindtap='uploadImg' wx:if="{{showBtn}}">
    <text>+</text>
  </view>
  <view class='item' wx:for="{{imagePaths}}" data-url="{{item}}" bindtap='previewImage'>
    <image src='{{item}}' mode='aspectFill' lazy-load="{{true}}"></image>
  </view>
</view>